import { Routes } from '@angular/router';

/**
 * Filled in as each domain's page component lands — T27 (simulator, the
 * root), T28 (signup, login, the guard), T29 (the wizard). Empty here on
 * purpose: T26 builds the shell before any page exists to route to, the same
 * way the backend's `main.py` accumulated routers one ticket at a time.
 */
export const routes: Routes = [];
