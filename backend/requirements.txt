# Runtime dependencies. The image installs this file and nothing else (DEP-052).
# Tooling lives in requirements-dev.txt and never ships.

fastapi>=0.115
uvicorn[standard]>=0.32

pydantic>=2.9
pydantic-settings>=2.6

sqlalchemy[asyncio]>=2.0.36
aiosqlite>=0.20
# SQLAlchemy's async layer bridges through greenlet. Without it the first query
# raises MissingGreenlet a long way from the cause.
greenlet>=3.1

argon2-cffi>=23.1
pyjwt>=2.9

# FastAPI does not depend on this. Without it every multipart/form-data request
# is rejected before a handler runs, and document upload fails with an error
# that never names the missing package.
python-multipart>=0.0.20

structlog>=24.4
opentelemetry-sdk>=1.28
opentelemetry-instrumentation-fastapi>=0.49b0
opentelemetry-exporter-otlp>=1.28

# Optional document classification, behind AI_CLASSIFICATION_ENABLED (AI-004).
# pdf2image shells out to pdftoppm; the system package is installed by the
# Dockerfile (DEP-053), not by pip.
anthropic>=0.40
pdf2image>=1.17
pillow>=11.0
